---
scrapeConfigs: null

serverFiles:
  prometheus.yml:
    scrape_configs:
      - job_name: "self"
        static_configs:
          - targets:
              - "localhost:9090"
            labels:
              foo: "bar"

alertmanager:
  enabled: false

kube-state-metrics:
  enabled: false

prometheus-node-exporter:
  enabled: false

prometheus-pushgateway:
  enabled: false
