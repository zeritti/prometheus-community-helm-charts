apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-graphite-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-graphite-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-graphite-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
